# Store Prompt Records for Debugging

Writer stores raw assembled prompt and model response records for agent sessions so a self-hosting author can debug poor model behavior, context assembly, and provider differences. Prompt records are hidden behind an advanced/debug view, must never include provider secrets, and need retention controls because they can contain substantial story project content.
