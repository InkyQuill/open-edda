# Tool-Accessible Whole-Project Context

Open Edda treats whole-project context as navigable access to the story project, not as an attempt to place every project document into every model prompt. Agent sessions receive project maps, retrieval, summaries, and read tools so they can inspect the full story project when needed while assembling task-focused context for each rewrite, generation, analysis, or brainstorming task.
