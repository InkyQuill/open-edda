# React Frontend for Galley Editor

Writer uses a React frontend because Galley Editor, the intended Markdown-native editing surface, is React-based. This keeps the chapter editor integration direct and makes it easier to improve the editor during service development instead of adapting it across frontend frameworks.
