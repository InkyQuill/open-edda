# Go Backend

Writer uses a Go backend with a React frontend. Go fits the self-hosted service shape, supports streaming model calls and agent tool workflows without heavy framework magic, handles Markdown import/export and archive operations well, and avoids the concerns the author has with Python and full-stack Next.js for this project.
