# Skill Scripts Require Admin Approval

Open Edda imports skill instructions, routing metadata, templates, and data files as usable agent context, but it does not execute arbitrary bundled skill scripts in the first version. Script execution is reserved for a later admin-approved permission model because self-hosted web services have a different security boundary than local terminal agents.
