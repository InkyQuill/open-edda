# OpenAI-Compatible Model Providers

Writer integrates with model services through an OpenAI-compatible API surface rather than targeting one provider-specific assistant platform. This keeps the product usable with services such as Ollama-compatible gateways, DeepSeek, OpenRouter, and future providers while requiring Writer to own prompt assembly, tool definitions, and agent-session behavior.
